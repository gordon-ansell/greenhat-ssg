{% import 'macros/meta.njk' as meta %}
<span class="meta">
    {%- if post.author -%}
        {{meta.authorsSilent(post.authorObjs)}}
    {%- endif -%}
    <meta name="dt-published" content="{{post.datePublished.iso}}" />
    <meta name="dt-modified" content="{{post.dateModified.iso}}" />
</span>