{%- if ctx.menus.bottom -%}
    <nav>
    <ul class="menu-plain">
        {%- for item in ctx.menus.bottom -%}
        <li><a href="{{ item.url }}"{% if item.description %} title="{{ item.description }}"{% endif %}>
            {{ item.title }}</a></li>
        {%- endfor -%}
    </ul>
    </nav>
{%- endif -%}{%- if site.copyright -%}
<div>
    {{site.copyright}}
</div>
{%- endif -%}